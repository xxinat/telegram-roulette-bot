"""
Структура проекта Telegram бота
"""

PROJECT_STRUCTURE = """
wwstrela/
│
├── bot/                               # Основной пакет бота
│   ├── __init__.py                   # Инициализация пакета
│   ├── main.py                       # ⭐ Главный файл - запуск бота
│   ├── config.py                     # ⭐ Конфигурация (токены, каналы, товары)
│   │
│   ├── database/                     # Работа с БД
│   │   ├── __init__.py
│   │   ├── models.py                 # SQLAlchemy модели (таблицы)
│   │   └── db_manager.py             # Менеджеры для CRUD операций
│   │
│   ├── handlers/                     # Обработчики команд и callback'ов
│   │   ├── __init__.py
│   │   ├── start.py                  # /start, проверка подписки
│   │   ├── menu.py                   # Главное меню
│   │   ├── shop.py                   # Магазин подарков
│   │   ├── roulette.py               # Рулетка
│   │   └── referral.py               # Реферальная программа
│   │
│   └── utils/                        # Утилиты
│       ├── __init__.py
│       ├── keyboards.py              # Клавиатуры (кнопки)
│       ├── subscription.py           # Проверка подписки на каналы
│       └── helpers.py                # Вспомогательные функции
│
├── requirements.txt                  # ⭐ Зависимости (pip install -r)
├── .env                              # ⭐ Переменные окружения (BOT_TOKEN и т.д)
│
├── README.md                         # Документация проекта
├── DEPLOYMENT.md                     # Инструкция по развёртыванию
├── EXAMPLES.py                       # Примеры расширенной конфигурации
├── PROJECT_STRUCTURE.md              # Описание файлов
│
└── bot_data.db                       # 📊 БД (создаётся автоматически)
"""

# ==================== ФАЙЛЫ И ИХ НАЗНАЧЕНИЕ ====================

FILES = {
    # === ОСНОВНЫЕ ФАЙЛЫ ===
    "bot/main.py": {
        "описание": "Главный файл бота",
        "содержит": ["Инициализация БД", "Создание бота", "Подключение обработчиков", "Запуск polling"],
        "запуск": "python bot/main.py"
    },
    "bot/config.py": {
        "описание": "Конфигурация бота",
        "содержит": ["BOT_TOKEN", "CHANNELS_TO_SUBSCRIBE", "SHOP_ITEMS", "ROULETTE_TICKETS", "ROULETTE_PRIZES"],
        "важное": "ОТРЕДАКТИРУЙТЕ ЭТО ФАЙЛ! Вставьте ваш BOT_TOKEN и ID каналов"
    },
    
    # === БД ===
    "bot/database/models.py": {
        "описание": "Модели БД",
        "таблицы": ["users", "purchases", "transactions", "referrals", "roulette_results"]
    },
    "bot/database/db_manager.py": {
        "описание": "Менеджеры для работы с БД",
        "классы": ["UserManager", "PurchaseManager", "ReferralManager", "RouletteManager"]
    },
    
    # === ОБРАБОТЧИКИ ===
    "bot/handlers/start.py": {
        "описание": "Команда /start и проверка подписки",
        "функции": ["Создание пользователя", "Проверка подписки", "Выбор маршрута"]
    },
    "bot/handlers/menu.py": {
        "описание": "Главное меню и навигация",
        "функции": ["Открытие магазина", "Рулетка", "Профиль", "Реферальная программа"]
    },
    "bot/handlers/shop.py": {
        "описание": "Магазин подарков",
        "функции": ["Отображение товаров", "Покупка товара", "Вычитание звёзд"]
    },
    "bot/handlers/roulette.py": {
        "описание": "Рулетка",
        "функции": ["Покупка билета", "Случайный выбор приза", "Выдача награды"]
    },
    "bot/handlers/referral.py": {
        "описание": "Реферальная программа",
        "функции": ["Реф. ссылка", "Отслеживание приглашений", "Медведи за приглашения"]
    },
    
    # === УТИЛИТЫ ===
    "bot/utils/keyboards.py": {
        "описание": "Клавиатуры и кнопки",
        "функции": ["Создание inline кнопок", "Навигация", "Меню выбора"]
    },
    "bot/utils/subscription.py": {
        "описание": "Проверка подписки",
        "функции": ["Проверка подписки через API", "Текст ошибок", "Форматирование сообщений"]
    },
    "bot/utils/helpers.py": {
        "описание": "Вспомогательные функции",
        "функции": ["Генерация кодов", "Форматирование", "Шаблоны текста"]
    },
    
    # === КОНФИГУРАЦИЯ ===
    "requirements.txt": {
        "описание": "Зависимости Python",
        "команда": "pip install -r requirements.txt"
    },
    ".env": {
        "описание": "Переменные окружения",
        "важное": "Отредактируйте перед запуском! Вставьте BOT_TOKEN"
    },
    
    # === ДОКУМЕНТАЦИЯ ===
    "README.md": {
        "описание": "Главная документация",
        "содержит": ["Обзор", "Установка", "Использование", "API"]
    },
    "DEPLOYMENT.md": {
        "описание": "Инструкция по развёртыванию",
        "содержит": ["Локальный запуск", "VPS", "Docker", "Heroku"]
    },
    "EXAMPLES.py": {
        "описание": "Примеры конфигурации",
        "содержит": ["Расширенные товары", "Специальные призы", "Сезонные скидки"]
    }
}

# ==================== БЫСТРЫЙ СТАРТ ====================

QUICK_START = """
1. УСТАНОВКА (5 минут):
   
   pip install -r requirements.txt

2. КОНФИГУРАЦИЯ:
   
   Отредактируйте bot/config.py:
   - Вставьте BOT_TOKEN
   - Добавьте ID каналов
   - Настройте товары/призы (опционально)

3. ЗАПУСК:
   
   python bot/main.py

4. ТЕСТИРОВАНИЕ:
   
   Откройте Telegram и напишите боту: /start
"""

# ==================== ФУНКЦИОНАЛЬНОСТЬ ====================

FEATURES = {
    "✅ Проверка подписки": {
        "файлы": ["handlers/start.py", "utils/subscription.py"],
        "как_работает": "При /start бот проверяет подписку на каналы из config.py"
    },
    
    "🎁 Магазин подарков": {
        "файлы": ["handlers/shop.py", "config.py"],
        "как_работает": "Пользователь выбирает товар и тратит звёзды"
    },
    
    "🎡 Рулетка": {
        "файлы": ["handlers/roulette.py", "config.py"],
        "как_работает": "Покупка билета → случайный выбор приза → получение награды"
    },
    
    "🐻 Реферальная программа": {
        "файлы": ["handlers/referral.py", "database/db_manager.py"],
        "как_работает": "Уникальная ссылка → приглашение друзей → 1 медведь за 5 друзей"
    },
    
    "💰 Система балансов": {
        "файлы": ["database/db_manager.py", "handlers/*"],
        "как_работает": "Звёзды (звёзды) и медведи (медведи) хранятся в БД"
    },
    
    "📊 БД и логирование": {
        "файлы": ["database/models.py", "database/db_manager.py"],
        "как_работает": "Все операции сохраняются в bot_data.db"
    }
}

# ==================== ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ ====================

ENV_VARIABLES = {
    "BOT_TOKEN": "Токен бота от @BotFather в Telegram",
    "CHANNEL_1_ID": "ID первого канала для подписки",
    "CHANNEL_1_NAME": "Username первого канала (@channel)",
    "CHANNEL_2_ID": "ID второго канала",
    "CHANNEL_2_NAME": "Username второго канала",
    "DATABASE_URL": "Путь к БД (по умолчанию sqlite:///bot_data.db)",
    "LOG_LEVEL": "Уровень логирования (INFO, DEBUG, WARNING)"
}

# ==================== ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ ====================

USAGE_EXAMPLES = """
# Получить пользователя
user = UserManager.get_or_create_user(telegram_id=12345)

# Добавить звёзды
UserManager.add_stars(telegram_id=12345, amount=10, description="Приз")

# Вычесть звёзды
success = UserManager.subtract_stars(telegram_id=12345, amount=5)

# Добавить медведей
UserManager.add_bears(telegram_id=12345, amount=1, description="Реф. приз")

# Проверить подписку
is_subscribed, not_subscribed = await check_subscription(bot, user_id)

# Добавить покупку
PurchaseManager.add_purchase(telegram_id, item_id, item_name, price, "shop")

# Добавить рефера
ReferralManager.add_referral(referrer_id, referred_user_id, username)
"""

# ==================== ТАБЛИЦЫ БД ====================

DATABASE_SCHEMA = """
TABLE users:
  - id (PRIMARY KEY)
  - telegram_id (UNIQUE)
  - username, first_name, last_name
  - stars, bears (баланс)
  - referral_code (UNIQUE)
  - referred_by
  - subscription_verified (BOOLEAN)
  - created_at, updated_at

TABLE purchases:
  - id (PRIMARY KEY)
  - user_id (FOREIGN KEY → users.id)
  - item_id, item_name, item_price
  - purchase_type ("shop" или "roulette")
  - created_at

TABLE transactions:
  - id (PRIMARY KEY)
  - user_id (FOREIGN KEY → users.id)
  - transaction_type (stars_earned, stars_spent, bears_earned, bears_spent)
  - amount, description
  - created_at

TABLE referrals:
  - id (PRIMARY KEY)
  - referrer_id (FOREIGN KEY → users.id)
  - referred_user_id
  - referred_username
  - bonus_paid (BOOLEAN)
  - created_at

TABLE roulette_results:
  - id (PRIMARY KEY)
  - user_id
  - ticket_id, ticket_name, ticket_price
  - prize_name, prize_type, prize_value
  - created_at
"""

# ==================== РАСШИРЕНИЕ ФУНКЦИОНАЛА ====================

EXTENSION_GUIDE = """
Как добавить новый товар:
1. Отредактируйте SHOP_ITEMS в bot/config.py
2. Добавьте: {"id": 6, "name": "Товар", "price": 100, "description": "..."}
3. Перезагрузите бота

Как добавить новую команду:
1. Создайте новый файл в bot/handlers/
2. Определите router и @router.message/callback_query обработчики
3. Подключите в bot/main.py: dp.include_router(...)

Как добавить новый уровень рулетки:
1. Отредактируйте ROULETTE_TICKETS в bot/config.py
2. Добавьте соответствующий уровень в ROULETTE_PRIZES
3. Перезагрузите бота

Как изменить БД:
1. Отредактируйте модели в bot/database/models.py
2. Удалите bot_data.db (она пересоздастся)
3. Перезагрузите бота
"""
"""

print("=" * 60)
print("🤖 ТЕЛЕГРАМ БОТ - СТРУКТУРА ПРОЕКТА")
print("=" * 60)
print(PROJECT_STRUCTURE)
print("\n" + "=" * 60)
print("⚡ БЫСТРЫЙ СТАРТ")
print("=" * 60)
print(QUICK_START)
