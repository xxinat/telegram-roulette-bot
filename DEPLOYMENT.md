# 📖 Инструкция по развёртыванию

## Быстрый старт (5 минут)

### Шаг 1: Получить BOT_TOKEN

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (придумайте имя и username)
4. BotFather даст вам **BOT_TOKEN** - скопируйте его

### Шаг 2: Создать каналы (опционально)

Если у вас есть каналы, найдите их ID:

```bash
# Способ 1: Отправьте сообщение в канал, затем используйте:
# https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates

# Способ 2: Используйте бота @userinfobot для получения ID канала
```

### Шаг 3: Установить и запустить

```bash
# 1. Перейти в папку проекта
cd c:\Users\Aliaksandr\Desktop\wwstrela

# 2. Установить зависимости
pip install -r requirements.txt

# 3. Отредактировать config.py
# Вставить ваш BOT_TOKEN и ID каналов

# 4. Запустить бот
python bot/main.py
```

## Детальная настройка

### 1. Конфигурация bot/config.py

```python
# ⬇️ Вставьте ваш токен
BOT_TOKEN = "123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11"

# ⬇️ Добавьте ID ваших каналов
CHANNELS_TO_SUBSCRIBE = [
    {
        "name": "Мой первый канал",
        "username": "@my_channel_1",
        "chat_id": -1001234567890  # Получить ID канала
    },
    {
        "name": "Мой второй канал",
        "username": "@my_channel_2",
        "chat_id": -1001234567891
    },
]

# ⬇️ Настройте товары в магазине
SHOP_ITEMS = [
    {"id": 1, "name": "Товар 1", "price": 10, "description": "Описание"},
    # Добавьте свои товары...
]

# ⬇️ Настройте билеты рулетки
ROULETTE_TICKETS = [
    {"id": 1, "name": "Дешёвый билет", "price": 10, "rarity": 1},
    # Добавьте свои билеты...
]
```

### 2. Найти ID канала

**Способ 1 (Рекомендуемый):**

1. Добавьте бота администратором в канал
2. Отправьте любое сообщение в канал
3. Перейдите по ссылке:
   ```
   https://api.telegram.org/botВАШ_BOT_TOKEN/getUpdates
   ```
4. Найдите в ответе `"chat":{"id":-1001234567890}`

**Способ 2:**

1. Откройте канал в Telegram Desktop
2. Нажмите правой кнопкой на название канала → Информация
3. В URL будет ID канала после `/c/`

**Способ 3:**

Используйте бота @userinfobot - отправьте ему сообщение в канале, и он выведет ID

### 3. Бюджет звёзд

Установите стартовый баланс для новых пользователей в `config.py`:

```python
STARTING_STARS = 100  # Количество звёзд при регистрации
```

### 4. Размер реф. бонуса

```python
REFERRAL_INVITES_PER_BEAR = 5  # 1 медведь за 5 приглашений
```

## Развёртывание на сервер

### Вариант 1: Linux/Ubuntu VPS

```bash
# 1. Подключиться к серверу
ssh root@your_server_ip

# 2. Установить Python и pip
apt update
apt install python3 python3-pip

# 3. Клонировать проект
git clone https://github.com/your_repo/wwstrela.git
cd wwstrela

# 4. Установить зависимости
pip install -r requirements.txt

# 5. Запустить бота в фоне (используя screen или systemd)
# Вариант A: screen
screen -S bot
python bot/main.py
# Нажмите Ctrl+A, затем Ctrl+D для выхода

# Вариант B: systemd (постоянная работа)
# Создайте файл /etc/systemd/system/telegram-bot.service:
```

**Файл /etc/systemd/system/telegram-bot.service:**

```ini
[Unit]
Description=Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/wwstrela
ExecStart=/usr/bin/python3 /root/wwstrela/bot/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```bash
# Запустить сервис
systemctl start telegram-bot
systemctl enable telegram-bot
systemctl status telegram-bot
```

### Вариант 2: Docker

**Dockerfile:**

```dockerfile
FROM python:3.9

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["python", "bot/main.py"]
```

**docker-compose.yml:**

```yaml
version: '3.8'

services:
  bot:
    build: .
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
    volumes:
      - ./bot_data.db:/app/bot_data.db
    restart: always
```

```bash
# Запустить
docker-compose up -d
```

### Вариант 3: Heroku (Бесплатный хостинг)

1. Создайте аккаунт на heroku.com
2. Установите Heroku CLI
3. В папке проекта создайте файл `Procfile`:

```
worker: python bot/main.py
```

4. Запустите:

```bash
heroku login
heroku create your-app-name
heroku config:set BOT_TOKEN="ваш_токен"
git push heroku main
```

## Мониторинг и логирование

### Просмотр логов (Linux)

```bash
# Если используется systemd
journalctl -u telegram-bot -f

# Если используется screen
screen -r bot
```

### Логирование в файл

Отредактируйте `bot/main.py`:

```python
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s - %(name)s - %(levelname)s - %(message)s",
    handlers=[
        logging.FileHandler("bot.log"),
        logging.StreamHandler()
    ]
)
```

## Решение проблем

### Ошибка: "Bot token is invalid"

- Проверьте, что BOT_TOKEN скопирован полностью
- Нет пробелов в начале/конце
- Перезагрузите бота

### Ошибка: "Chat not found"

- Проверьте ID канала (должен быть отрицательный: -100...)
- Убедитесь, что бот администратор в канале

### Ошибка: "User not found" при покупке

- Проверьте логи БД
- Убедитесь, что пользователь завершил /start

### БД заполнена ошибками

Очистить БД можно:

```bash
# Удалить файл БД
rm bot_data.db

# Бот создаст новую при следующем запуске
python bot/main.py
```

## Безопасность в production

1. **Используйте переменные окружения:**

```bash
export BOT_TOKEN="your_token"
export DATABASE_URL="sqlite:///bot_data.db"
```

2. **Ограничьте доступ к файлам:**

```bash
chmod 600 .env
chmod 700 bot_data.db
```

3. **Используйте SSL для БД** (если используется удаленная БД)

4. **Регулярно делайте бэкапы:**

```bash
# Скрипт для автоматического бэкапа (cron job)
0 3 * * * cp /root/wwstrela/bot_data.db /root/backups/bot_data_$(date +%Y%m%d).db
```

## Версионирование и обновления

### Обновить код с GitHub

```bash
git pull origin main
pip install -r requirements.txt
systemctl restart telegram-bot
```

### Откатить на предыдущую версию

```bash
git log --oneline  # Посмотреть историю
git revert COMMIT_ID
git push origin main
```

## FAQ

**Q: Как изменить товары в магазине?**  
A: Отредактируйте `SHOP_ITEMS` в `config.py` и перезагрузите бота

**Q: Как экспортировать данные из БД?**  
A: БД находится в файле `bot_data.db`. Используйте SQLite Browser или:

```bash
sqlite3 bot_data.db ".dump" > backup.sql
```

**Q: Бот не отвечает на команды**  
A: Проверьте:
- Логи (`python bot/main.py`)
- BOT_TOKEN корректен
- Интернет соединение

**Q: Как добавить администратора?**  
A: Отредактируйте `config.py` и добавьте telegram_id админа в список ADMINS

---

**Нужна помощь?** Проверьте логи и README.md
